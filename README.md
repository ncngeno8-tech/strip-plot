# strip-plot
This repository includes a strip plot example using R and ggplot2.
Dataset used: mtcars (built-in R dataset).
The x-axis groups cars by cylinder count, and the y-axis shows miles per gallon.
I used jitter so the points don’t overlap, which makes the distribution easier to see. 
